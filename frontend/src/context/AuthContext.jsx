import { createContext, useContext, useMemo, useState } from 'react'
import { authApi } from '../services/authApi'
import { storage } from '../services/api'
const AuthContext = createContext(null)
export function AuthProvider({ children }) { const [userType, setUserType] = useState(localStorage.getItem('simplecart_user_type')); const [user, setUser] = useState(() => { try { return JSON.parse(localStorage.getItem('simplecart_user') || 'null') } catch { return null } }); const login = async (body, type) => { const data = await authApi.login(body, type); setUserType(type); setUser({ email: data.email || body.email }); localStorage.setItem('simplecart_user_type', type); localStorage.setItem('simplecart_user', JSON.stringify({ email: data.email || body.email })) }; const register = async (body, type) => { await authApi.register(body, type); return login({ name: body.name, email: body.email, password: body.password }, type) }; const logout = () => { storage.clear(); localStorage.removeItem('simplecart_admin_access'); localStorage.removeItem('simplecart_admin_refresh'); localStorage.removeItem('simplecart_user_type'); localStorage.removeItem('simplecart_user'); setUser(null); setUserType(null) }; const value = useMemo(() => ({ user, userType, isAuthenticated: Boolean(userType && (userType === 'admin' ? localStorage.getItem('simplecart_admin_access') : storage.access)), accessToken: userType === 'admin' ? localStorage.getItem('simplecart_admin_access') : storage.access, login, logout, register, refreshSession: () => Promise.resolve() }), [user, userType]); return <AuthContext.Provider value={value}>{children}</AuthContext.Provider> }
export const useAuth = () => useContext(AuthContext)
